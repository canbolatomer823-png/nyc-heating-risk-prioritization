apiVersion: v1
kind: Service
metadata:
  name: nhr-api
  namespace: nyc-heat-risk
spec:
  type: LoadBalancer
  selector:
    app: nhr-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
